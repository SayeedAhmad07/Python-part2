class solution:
    def gcd(self,a,b):
        if a==0:
            return b
        return gcd(b%a,a)


class Solution:
    def findGCD(self, nums: List[int]) -> int:
        a=max(nums)
        b=min(nums)
        return gcd(a,b)
