from itertools import permutations
class Solution:
    def permute(self, nums: List[int]) -> List[List[int]]:
        p=permutations(nums)
        m=[]
        for i in list(p):
            x=list(i)
            s=[]
            for j in x:
                s.append(j)
            m.append(s)
        return m
